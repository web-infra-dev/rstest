# exclude

- **类型：** `string[]`
- **默认值：** `['**/node_modules/**', '**/dist/**', '**/.{idea,git,cache,output,temp}/**']`

匹配 glob 规则的文件将不会被作为测试文件。

```ts
import { defineConfig } from 'rstest';

export default defineConfig({
  exclude: ['**/node_modules/**'],
});
```
