# 浏览器模式

Rstest 提供了浏览器模式（Browser Mode），允许你在真实浏览器中运行测试，而不是使用 jsdom 或 happy-dom 等模拟环境。

## 适用场景（Component Testing）

浏览器模式特别适合用于 **Component Testing** 中那些需要“真实浏览器行为”的用例，例如：

- 依赖真实 CSS/layout 计算（例如 `getComputedStyle`、字体/滚动/定位）
- 依赖真实事件与焦点行为（pointer/keyboard/focus/selection）
- 依赖 Canvas / WebGL / Web Workers 等 jsdom 难以完整模拟的能力
- 需要跨浏览器验证一致性（Chromium / Firefox / WebKit）

## 什么是浏览器模式？

浏览器模式使用 [Playwright](https://playwright.dev/) 在真实浏览器（Chromium、Firefox 或 WebKit）中执行你的测试代码。这意味着你的测试会在更接近生产环境的浏览器 API 和行为下运行。

## 何时使用浏览器模式

使用以下决策树来判断是否需要浏览器模式：

```
依赖真实浏览器 API? ─── 是 ─▶ ✅ 浏览器模式
         │ 否
         ▼
需要跨浏览器测试?  ─── 是 ─▶ ✅ 浏览器模式
         │ 否
         ▼
jsdom 中行为异常?  ─── 是 ─▶ ✅ 浏览器模式
         │ 否
         ▼
    💡 可用 jsdom
```

:::tip 建议策略
浏览器模式通常比 jsdom 更慢、资源消耗更高。推荐将它用于“关键交互/布局相关组件”的用例，其余单元测试仍然使用 jsdom 以控制整体耗时。
:::

## 浏览器模式 vs. jsdom / happy-dom

以下是浏览器模式与传统模拟环境的对比：

| 特性              | 浏览器模式      | jsdom / happy-dom      |
| ----------------- | --------------- | ---------------------- |
| 浏览器 API 完整性 | ✅ 完整支持     | ⚠️ 部分模拟            |
| Canvas / WebGL    | ✅ 原生支持     | ❌ 不支持或需 polyfill |
| CSS 计算样式      | ✅ 真实渲染     | ⚠️ 有限支持            |
| Web Workers       | ✅ 原生支持     | ❌ 不支持              |
| 执行速度          | ⚠️ 较慢         | ✅ 更快                |
| 资源消耗          | ⚠️ 较高         | ✅ 较低                |
| 调试体验          | ✅ 可视化调试   | ⚠️ 仅控制台            |
| 跨浏览器测试      | ✅ 支持多浏览器 | ❌ 不支持              |

## 下一步

- [快速开始](/guide/browser-testing/getting-started) - 配置并运行你的第一个浏览器测试
- [组件测试](/guide/browser-testing/testing-components) - 在浏览器模式下进行 Component Testing（推荐 Testing Library）
- [browser 配置](/config/test/browser) - 查阅浏览器模式配置项与常用配置
