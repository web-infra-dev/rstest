import { describe, expect, it } from '@rstest/core';

describe('disk sum', () => {
  it('1 + 2 = 3', () => {
    expect(1 + 2).toBe(3);
  });

  it('passes second case', () => {
    expect('hello').toBe('hello');
  });
});
