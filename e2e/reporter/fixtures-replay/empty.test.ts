// No tests on purpose: the file still reports `onTestFileStart`/`Ready`/
// `Result` and its module-level output, all with an empty collected tree.
console.log('module-level log from a file with no tests');
