import { describe, expect, it } from '@rstest/core';

describe('agent-md', () => {
  it('truncated case 1', () => {
    expect(1).toBe(2);
  });

  it('truncated case 2', () => {
    expect(2).toBe(3);
  });

  it('truncated case 3', () => {
    expect(3).toBe(4);
  });

  it('truncated case 4', () => {
    expect(4).toBe(5);
  });

  it('truncated case 5', () => {
    expect(5).toBe(6);
  });
});
