{
  "name": "rstest_example",
  "description": "A complete Module Federation Case with React.",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "pnpm --filter rstest_example_* --parallel start",
    "build": "pnpm --filter rstest_example_* --parallel build",
    "test": "pnpm --filter rstest_example_main-app test && pnpm --filter rstest_example_component-app test",
    "e2e:ci": "pnpm build && pnpm exec playwright test --reporter=list"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/anderlaw/react-webpack-MF/issues"
  },
  "homepage": "https://github.com/anderlaw/react-webpack-MF#readme",
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "playwright": "^1.49.1"
  }
}
