{
  "name": "@examples/federation",
  "version": "0.0.0",
  "private": true,
  "description": "A complete Module Federation Case with React.",
  "keywords": [],
  "homepage": "https://github.com/web-infra-dev/rstest/tree/main/examples/federation",
  "bugs": {
    "url": "https://github.com/web-infra-dev/rstest/issues"
  },
  "license": "ISC",
  "author": "",
  "scripts": {
    "build": "pnpm --filter @examples/federation-main-app --filter @examples/federation-component-app --filter @examples/federation-node-local-remote --parallel build",
    "e2e:ci": "pnpm build && pnpm exec playwright test --reporter=list",
    "start": "pnpm --filter @examples/federation-main-app --filter @examples/federation-component-app --parallel start",
    "test": "pnpm --filter @examples/federation-main-app test && pnpm --filter @examples/federation-component-app test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "playwright": "^1.60.0"
  }
}
