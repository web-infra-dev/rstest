## API Report File for "@rstest/browser-react"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { JSXElementConstructor } from 'react';
import type { ReactNode } from 'react';

// Warning: (ae-forgotten-export) The symbol "ActFunction" needs to be exported by the entry point index.d.ts
//
// @public
export const act: ActFunction;

// @public
export function cleanup(): Promise<void>;

// @public
export function configure(customConfig: Partial<RenderConfiguration>): void;

// @public
export function render(ui: ReactNode, options?: RenderOptions): Promise<RenderResult>;

// @public (undocumented)
export interface RenderConfiguration {
    reactStrictMode: boolean;
}

// @public
export function renderHook<Props, Result>(renderCallback: (props?: Props) => Result, options?: RenderHookOptions<Props>): Promise<RenderHookResult<Result, Props>>;

// @public (undocumented)
export interface RenderHookOptions<Props> extends RenderOptions {
    initialProps?: Props;
}

// @public (undocumented)
export interface RenderHookResult<Result, Props> {
    act: (callback: () => unknown) => Promise<void>;
    rerender: (props?: Props) => Promise<void>;
    result: {
        current: Result;
    };
    unmount: () => Promise<void>;
}

// @public (undocumented)
export interface RenderOptions {
    baseElement?: HTMLElement;
    container?: HTMLElement;
    wrapper?: JSXElementConstructor<{
        children: ReactNode;
    }>;
}

// @public (undocumented)
export interface RenderResult {
    asFragment: () => DocumentFragment;
    baseElement: HTMLElement;
    container: HTMLElement;
    rerender: (ui: ReactNode) => Promise<void>;
    unmount: () => Promise<void>;
}

// (No @packageDocumentation comment for this package)

```
