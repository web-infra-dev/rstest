// JavaScript test file to verify discovery of *.spec.* patterns
import { describe, expect, it } from '@rstest/core';

describe('JS', () => {
  it('should run in JS', () => {
    expect(1).toBe(1);
  });
});
